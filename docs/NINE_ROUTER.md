# NINE_ROUTER

## Requirements

- Model discovery MUST query the `/models` endpoint.
- Results MUST be grouped by provider.

_(Content intentionally left as a stub — full documentation to be authored later.)_
